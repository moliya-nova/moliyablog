<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bloger.backend.mapper.CarouselSlideMapper">
    <select id="selectAll" resultType="com.bloger.backend.entity.CarouselSlide">
        SELECT * FROM carousel_slide WHERE status = 1 ORDER BY sort ASC
    </select>
    
    <select id="selectById" parameterType="java.lang.Long" resultType="com.bloger.backend.entity.CarouselSlide">
        SELECT * FROM carousel_slide WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.bloger.backend.entity.CarouselSlide">
        INSERT INTO carousel_slide (image_url, title, category, sort, status, create_time, update_time)
        VALUES (#{imageUrl}, #{title}, #{category}, #{sort}, #{status}, NOW(), NOW())
    </insert>
    
    <update id="update" parameterType="com.bloger.backend.entity.CarouselSlide">
        UPDATE carousel_slide
        SET image_url = #{imageUrl}, title = #{title}, category = #{category}, sort = #{sort}, status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM carousel_slide WHERE id = #{id}
    </delete>
</mapper>
